
<% #([:admin, @book], html: {id: "form_book"}) in bisa update %>
<%= form_for(@book, url: admin_books_path, html: {id: "form_book", multipart: true}) do |f| %>

<% if @book.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>

    <ul>
    <% @book.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<div class="field">
  <%= f.label :name %><br />
  <%= f.text_field :name, required: true %>
</div>
  <%= select("book","category", Category.all.collect {|p| [ p.name, p.id ] }, {include_blank: true}) %>
  <%= f.date_field :date_release %>
  <%= f.file_field :photo %>
<div class="actions">
  <%= f.submit %>
</div>
<% end %> 